CREATE SEQUENCE cliente_id_seq AS BIGINT START WITH 1 INCREMENT BY 1;
CREATE TABLE cliente (
  id BIGINT GENERATED BY DEFAULT AS SEQUENCE cliente_id_seq PRIMARY KEY,
  nome VARCHAR(100) NOT NULL,
  email VARCHAR(100) NOT NULL,
  data_de_nascimento DATE NOT NULL
);